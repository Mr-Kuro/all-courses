/*

const input = document.getElementById('input') as HTMLInputElement;

input.addEventListener('input', (evenet) => {
    // console.log('Digitei');
    const i = evenet.currentTarget as HTMLInputElement;
    console.log(i.value)
    // console.log(event?.currentTarget.
});

*/